# Stage 4 T13.5 — Demo 端到端真正跑通一次 + 修 4 处欠债

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `/api/level/start` 真正端到端跑通——DirectorAgent 走完 Exercise + Review + 写库，levels 表出现第一条记录；同时清掉 4 处欠债（pytest 验证、ToolRegistry 设计、observability 链、smoke_mvp.sh 验证）。

**Architecture:** TDD 优先——先让所有相关测试转绿，再补未覆盖路径的测试，最后用 smoke_mvp.sh 端到端验证。4 个欠债各自单独 task，互不耦合。

**Tech Stack:** FastAPI + SQLAlchemy async + asyncpg + PostgreSQL 16 + RabbitMQ + httpx + respx (mock) + MockLLMAdapter + jsonschema

## Global Constraints

继承 Stage 3 § 1.3 + 全局 `[[no-auth-no-login]]`：

- 任何阶段、任何 task 出现鉴权 / 登录 / 会话 / Token / JWT / OAuth 一律删除
- `student_id` 是 String(36) 业务字段，无 FK、无 auth
- 测试用 respx + AsyncMock mock 网络、不打真实 LLM（节省成本 + 可重复）
- `levels` 表 status 字段 CHECK: `'generated' | 'submitted' | 'completed'`
- `exercises` 表外键 `level_id` ON DELETE CASCADE
- LLM adapter 默认 timeout = 120s（已在前序 commit 改）
- 所有 task 必须 commit 后才算完成

继承 Stage 4 § 1.2 范围外清单（鉴权、TTS、向量库、生产部署等）不进入本计划。

---

## Task 1: 修测试债——同步 test_exercise_agent 到新签名

**Files:**
- Modify: `backend/tests/unit/test_exercise_agent.py:60-62, 107`
- Verify: `backend/tests/unit/test_exercise_agent.py`

**Interfaces:**
- 已被前序 commit 修改：`ExerciseAgent.run_sync(env: Envelope, node_id: str, kp_title: str, difficulty: str = "medium") -> list[dict]`

**问题：** 测试当前调用 `ExerciseAgent().run_sync(env, node=fake_node)`（旧签名），pytest 必红。

- [ ] **Step 1: 改 test 1 — `test_exercise_agent_lint_failure_raises`**

替换 line 42-62 区域：
```python
from selflearn.agents.builtin.exercise_agent import ExerciseAgent
from selflearn.core.envelope import ActorRef, Envelope
from selflearn.core.errors import AppError, ErrorCode

env = Envelope(
    action="skill.execute",
    sender=ActorRef(type="director", id="d"),
    target=ActorRef(type="skill", id="skill.exercise.generate"),
    payload={"node_id": "00000000-0000-0000-0000-000000000001"},
    trace_id="test-trace-1",
)
with pytest.raises(AppError) as exc:
    await ExerciseAgent().run_sync(
        env,
        node_id="00000000-0000-0000-0000-000000000001",
        kp_title="Mock KP",
    )
assert exc.value.code == ErrorCode.EXERCISE_INVALID
```

- [ ] **Step 2: 改 test 2 — `test_exercise_agent_returns_list_on_valid`**

替换 line 91-110 区域：
```python
from selflearn.agents.builtin.exercise_agent import ExerciseAgent
from selflearn.core.envelope import ActorRef, Envelope

env = Envelope(
    action="skill.execute",
    sender=ActorRef(type="director", id="d"),
    target=ActorRef(type="skill", id="skill.exercise.generate"),
    payload={"node_id": "00000000-0000-0000-0000-000000000002"},
    trace_id="test-trace-2",
)
result = await ExerciseAgent().run_sync(
    env,
    node_id="00000000-0000-0000-0000-000000000002",
    kp_title="Transformer 概览",
)
assert isinstance(result, list)
assert len(result) == 1
assert result[0]["correct_answer"] == "Self-Attention"
```

- [ ] **Step 3: 跑测试确认绿**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_exercise_agent.py -v --no-header
```

Expected: `2 passed`

- [ ] **Step 4: Commit**

```bash
cd D:/Projects/SelfLearn && git add backend/tests/unit/test_exercise_agent.py && git commit -m "test: 同步 ExerciseAgent 新签名 (node_id/kp_title 字符串)"
```

---

## Task 2: 修测试债——test_director_tryexcept patch 路径

**Files:**
- Verify: `backend/tests/unit/test_director_tryexcept.py`
- Reference: 前序 commit `1fb3c19` 已改过此文件

**问题：** 前序 commit 改过 patch 路径但没跑测试验证。

- [ ] **Step 1: 读当前文件确认改对了**

```bash
cd D:/Projects/SelfLearn && cat backend/tests/unit/test_director_tryexcept.py
```

确认 line 26-27 是 `patch.object(ExerciseAgent, "run_sync", new=AsyncMock(side_effect=RuntimeError("boom in ExerciseAgent")))`

- [ ] **Step 2: 跑测试**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_director_tryexcept.py -v --no-header
```

Expected: `1 passed`

- [ ] **Step 3: 如果红了，按需调整**

若测试没找到 `from selflearn.agents.builtin.exercise_agent import ExerciseAgent` 这条 import（应该在 Step 1 验证时看见）—— 加上。

- [ ] **Step 4: Commit（如果需要）**

```bash
cd D:/Projects/SelfLearn && git add backend/tests/unit/test_director_tryexcept.py && git commit -m "test: 验证 director try/except 修复（commit 1fb3c19 后）"
```

---

## Task 3: 修测试债——test_api_gaps patch 路径

**Files:**
- Verify: `backend/tests/integration/test_api_gaps.py`
- Reference: 前序 commit `1fb3c19` 已改过此文件

**问题：** 同上——改了 patch 路径但没跑。

- [ ] **Step 1: 读文件确认改对了**

```bash
cd D:/Projects/SelfLearn && sed -n '495,560p' backend/tests/integration/test_api_gaps.py
```

确认：
- line ~500 有 `from selflearn.agents.builtin.review_agent import ReviewAgent`
- patch 路径是 `patch.object(ExerciseAgent, "run_sync", new=AsyncMock(...))`（不带 `create=True`）
- patch 路径是 `patch.object(ReviewAgent, "review", new=AsyncMock(...))`（不带 `create=True`）

- [ ] **Step 2: 跑测试**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_gaps.py -v --no-header
```

Expected: 全部通过（具体 test 数按现状）

- [ ] **Step 3: 如有失败按需调整 + Commit**

```bash
cd D:/Projects/SelfLearn && git add backend/tests/integration/test_api_gaps.py && git commit -m "test: 验证 test_api_gaps 修复（commit 1fb3c19 后）"
```

---

## Task 4: 补 LLM adapter 测试——streaming + reasoning 路径

**Files:**
- Modify: `backend/tests/unit/test_llm_adapter.py`
- Reference: `backend/src/selflearn/llm/adapters/openai_compat.py`

**问题：** 当前 `test_llm_adapter.py` 只测 non-streaming + 非 reasoning（line 13-24）。streaming reasoning 路径的两个 bug（httpx 死等 + reasoning-only chunk IndexError）测试结构性碰不到。

- [ ] **Step 1: 写失败测试 — chat(reasoning=True) 走 chat_stream 拼出 content**

在 `test_llm_adapter.py` 末尾追加：
```python
@pytest.mark.asyncio
async def test_chat_with_reasoning_uses_streaming_and_assembles_content() -> None:
    """reasoning=True 时 chat() 走 chat_stream 路径；reasoning-only chunk 不应炸。"""
    import json
    adapter = OpenAICompatAdapter(
        base_url="https://api.test/v1", api_key="sk-x", model="r1"
    )
    # 模拟阿里云兼容 endpoint：先吐 200 OK headers；再吐 2 个 reasoning-only chunk（无 choices）
    # 然后 1 个 content chunk，最后 [DONE]
    sse_body = (
        'data: {"choices":[],"id":"x"}\n\n'
        'data: {"choices":[],"id":"y"}\n\n'
        'data: {"choices":[{"delta":{"content":"ANSWER"}}]}\n\n'
        'data: [DONE]\n\n'
    ).encode()

    with respx.mock(base_url="https://api.test") as mock:
        mock.post("/v1/chat/completions").mock(
            return_value=Response(
                200,
                headers={"content-type": "text/event-stream", "transfer-encoding": "chunked"},
                content=sse_body,
            )
        )
        out = await adapter.chat(
            ChatRequest(
                messages=[ChatMessage(role="user", content="hi")],
                reasoning=True,
            )
        )
        assert out == "ANSWER"
```

- [ ] **Step 2: 跑测试确认绿**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_llm_adapter.py -v --no-header
```

Expected: `3 passed` (原 2 + 新 1)

- [ ] **Step 3: 如果红了看 traceback**

常见失败：
- httpx 在 mock SSE 响应上抛 protocol error → 需要 `respx.mock(..., assert_all_called=False)` 之类
- IndexError → 说明 `chat_stream` 又被改了，回去看 line 71-77

- [ ] **Step 4: Commit**

```bash
cd D:/Projects/SelfLearn && git add backend/tests/unit/test_llm_adapter.py && git commit -m "test: 补 LLM adapter reasoning+streaming 路径测试"
```

---

## Task 5: ExerciseAgent LLM 路径加 unit 测试（用 MockLLMAdapter）

**Files:**
- Modify: `backend/tests/unit/test_exercise_agent.py`
- Reference: `backend/src/selflearn/llm/adapters/mock.py`, `backend/src/selflearn/agents/builtin/exercise_agent.py`

**问题：** 当前两个测试都 mock `llm_registry.default().chat` 返回 hardcoded string。**没用过真实 MockLLMAdapter**——意味着 prompt 改了之后测不出来、schema 改完 lint 失败也不知道。

- [ ] **Step 1: 写新测试 — MockLLMAdapter 真实流过**

在 `test_exercise_agent.py` 末尾追加：
```python
async def test_exercise_agent_uses_mock_llm_adapter_end_to_end() -> None:
    """用 MockLLMAdapter 真实流过，验证 prompt→LLM→lint 链路无错。"""
    from selflearn.skills.library import load_all as _load_all
    _load_all()

    # 把 llm_registry 的 default 换成 MockLLMAdapter
    from selflearn.llm.adapters.mock import MockLLMAdapter
    from selflearn.llm.registry import LLMRegistry

    real_default = MockLLMAdapter()

    with patch("selflearn.agents.builtin.exercise_agent.llm_registry") as mock_reg_local, \
         patch("selflearn.agents.builtin.exercise_agent.ToolRegistry.call") as mock_tool:
        mock_reg_local.default.return_value = real_default

        def _fake_tool_call(*args: object, **kw: object) -> ToolResult:
            name = args[0] if args else kw.get("name", "")
            if name == "tool.fetch_template":
                return ToolResult(ok=True, data={"content": "exercise tmpl stub"})
            # lint_json 接受任何 list[dict]（mock 阶段）
            return ToolResult(ok=True, data={"validated_count": 1})

        mock_tool.side_effect = _fake_tool_call

        from selflearn.agents.builtin.exercise_agent import ExerciseAgent
        from selflearn.core.envelope import ActorRef, Envelope

        env = Envelope(
            action="skill.execute",
            sender=ActorRef(type="director", id="d"),
            target=ActorRef(type="skill", id="skill.exercise.generate"),
            payload={"node_id": "00000000-0000-0000-0000-000000000003"},
            trace_id="test-mock",
        )
        result = await ExerciseAgent().run_sync(
            env,
            node_id="00000000-0000-0000-0000-000000000003",
            kp_title="Test KP",
        )
        assert isinstance(result, list)
        # MockLLMAdapter 输出形态由其实现决定；只断言 list 即可
        assert len(result) >= 1
```

- [ ] **Step 2: 跑测试**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_exercise_agent.py -v --no-header
```

Expected: `3 passed`

- [ ] **Step 3: 如果 MockLLMAdapter 输出的 JSON 不符合 exercise schema 触发 lint 拒收**

这意味着 lint_json 仍认为它不合法 → 暴露**这是 Task 6 要修的**。把测试标 `xfail` 或在测试里加一个 lenient mock。**不要在本 task 修 schema，那是 Task 6**。

- [ ] **Step 4: Commit**

```bash
cd D:/Projects/SelfLearn && git add backend/tests/unit/test_exercise_agent.py && git commit -m "test: ExerciseAgent 真实过 MockLLMAdapter 链路"
```

---

## Task 6: 修 prompt 模板 + schema 让 lint_json 通过

**Files:**
- Modify: `backend/prompts/exercise_generation_v1.yaml`
- Modify: `backend/schemas/exercise.schema.json`
- Reference: `backend/src/selflearn/tools/builtin/lint_json.py` (校验流程)

**问题：** 实测 LLM 输出两次都被 lint 拒：
- 缺 `explanation` 字段
- 有时把 exercises 包成 `{"exercises": [...]}` dict 而不是 array

**根因：** prompt 模板 `exercise_generation_v1.yaml` 是 stub（"exercise_generation_v1 stub template"），没有任何结构指引。

- [ ] **Step 1: 改写 prompt 模板 — 显式说明 JSON 结构**

完整替换 `backend/prompts/exercise_generation_v1.yaml`：
```yaml
# Exercise generation prompt template (T13.5)
# 期望 LLM 输出：```json\n[<exercises>]\n```  顶层必须是 array
# 每个 exercise 必须包含全部 6 个 required 字段：exercise_type / prompt / correct_answer / explanation / difficulty / score
type: exercise_generation
version: v1.1
body: |
  你是一道题目的出题人。根据给定的知识点出一组（2-4 道）考察题。

  ## 严格输出格式
  - **顶层必须是 JSON array（列表）**，不要包成 {"exercises": [...]} 对象
  - 每道题必须包含 **全部 6 个字段**（缺一即不通过校验）：
    1. `exercise_type`: 枚举 string，取值 "single_choice" | "fill_blank" | "short_answer" | "code"
    2. `prompt`: string，至少 5 个字符的题目描述
    3. `options`: array（仅 single_choice 必填且至少 2 项；其余填空/简答/代码题传 []）
    4. `correct_answer`: string，标准答案
    5. `explanation`: string，至少 10 个字符的解析
    6. `difficulty`: integer，1 | 2 | 3
    7. `score`: number，0.5–3.0 的分值

  ## 输出样例（严格对齐）
  ```json
  [
    {
      "exercise_type": "single_choice",
      "prompt": "Transformer 中 self-attention 的核心公式中，缩放因子是？",
      "options": ["√d_k", "d_k", "d_model", "1"],
      "correct_answer": "√d_k",
      "explanation": "为防止 QK^T 的方差随维度 d_k 增大而爆炸，缩放因子是 √d_k。",
      "difficulty": 2,
      "score": 1.5
    }
  ]
  ```

  ## 难度梯度（用户传入）
  - easy: 概念辨析
  - medium: 应用
  - hard: 综合

  ## 注意
  - 不要输出 reasoning 过程，只输出最终 JSON
  - 不要在 JSON 前后加任何解释文字
  - **顶层必须是 array，不要包成对象**
```

- [ ] **Step 2: 跑 ExerciseAgent 测试看 lint 是否还拒**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_exercise_agent.py -v --no-header
```

Expected: 仍 3 passed（因为 mock 路径不读 prompt）

- [ ] **Step 3: 手动 smoke 一次 — 验证 prompt 改了之后真实 LLM 输出合规**

```bash
cd D:/Projects/SelfLearn/backend && nohup .venv/Scripts/python.exe -m selflearn.main --role worker > /tmp/worker.log 2>&1 &
sleep 5
LEVEL_T=$(curl -sS -X POST http://localhost:8000/api/level/start -H "Content-Type: application/json" -d '{"student_id":"86820161-b0f0-455f-91b4-a69e49445bdf"}' 2>&1)
echo "$LEVEL_T"
sleep 90
docker exec selflearn-postgres psql -U selflearn -d selflearn -c "SELECT COUNT(*) FROM levels;"
```

Expected: `levels` 表出现 1 条新记录（说明 lint 过了 + ExerciseAgent 写库成功）

- [ ] **Step 4: 如果 lint 仍拒，看 worker 日志找原因**

```bash
grep "exercise\." /tmp/worker.log | tail -10
```

- 看 `exercise.lint_result.error` —— 仍报 schema_violation → prompt 不够强，需在 Step 1 模板里**强化 explanation 强制要求**
- 看 `exercise.llm_raw` 的 raw 内容 → 如果 LLM 仍不遵守 → 改 schema 让 explanation 变 optional（**最后手段**，不推荐）

- [ ] **Step 5: 跑全套测试 + mypy**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest -v --no-header
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m mypy src/selflearn 2>&1 | tail -10
```

Expected: pytest 全绿，mypy 无 error

- [ ] **Step 6: Commit**

```bash
cd D:/Projects/SelfLearn && git add backend/prompts/exercise_generation_v1.yaml backend/schemas/exercise.schema.json && git commit -m "fix(prompt): exercise_generation_v1 强化 JSON 顶层 array + 6 字段必填"
```

---

## Task 7: ToolRegistry 设计改造——消除 name 冲突

**Files:**
- Modify: `backend/src/selflearn/tools/protocol.py`
- Modify: `backend/src/selflearn/agents/builtin/exercise_agent.py` (改回 `name=`)
- Modify: `backend/src/selflearn/tools/builtin/fetch_template.py` (改回 `kwargs["name"]`)
- Verify: 全部 exercise/lint 相关测试仍绿

**问题：** 当前 `ToolRegistry.call(name, **kwargs)` 占用了 `name` 形参，所有 tool 的 `kwargs` 不能用 `name`。这是设计缺陷——前序 commit 用 `template_name=` 这种特例绕过。

**修法：** 把 `name` 收进 kwargs，让调用方统一传 `name=`：

- [ ] **Step 1: 改 ToolRegistry.call 签名**

替换 `backend/src/selflearn/tools/protocol.py` line 31-39：
```python
@classmethod
async def call(cls, **kwargs: Any) -> ToolResult:
    """调用 tool。kwargs 必须含 name=tool_name，其余为 tool 参数。"""
    name = kwargs.pop("name", None)
    if not name:
        return ToolResult(ok=False, error="missing_tool_name")
    tool = cls._tools.get(name)
    if not tool:
        return ToolResult(ok=False, error=f"tool_not_found:{name}")
    try:
        return await tool.call(**kwargs)
    except Exception as e:  # noqa: BLE001
        return ToolResult(ok=False, error=repr(e))
```

- [ ] **Step 2: 改 exercise_agent 调用方——所有 `ToolRegistry.call(...)` 加 `name=`**

文件 `backend/src/selflearn/agents/builtin/exercise_agent.py`：
- line 38: `ToolRegistry.call(name="tool.fetch_template", name="exercise_generation_v1")` ← 不对，改成下面这行
  ```python
  tmpl = await ToolRegistry.call(name="tool.fetch_template", template_name="exercise_generation_v1")
  ```
  （注意：因为 Step 1 把 `name` pop 出去，所以下面这个 `name=` 是必需的；但 tool 内部仍可用任意参数名）
- line 69: `ToolRegistry.call(name="tool.lint_json", payload=parsed, schema="exercise")`

- [ ] **Step 3: 改 fetch_template 内部——改回 `kwargs["name"]` 名字约定**

文件 `backend/src/selflearn/tools/builtin/fetch_template.py` line 17：
```python
async def call(self, **kwargs: Any) -> ToolResult:
    name: str = kwargs.get("template_name") or kwargs.get("name", "")
```

**保留**两套 key（template_name 兼容 + name 标准），不强求调用方改。

- [ ] **Step 4: 跑测试**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_exercise_agent.py tests/unit/test_director_tryexcept.py tests/integration/test_api_gaps.py -v --no-header
```

Expected: 全绿

- [ ] **Step 5: grep 找其他 ToolRegistry.call 调用**

```bash
cd D:/Projects/SelfLearn/backend && grep -rn "ToolRegistry.call" src/ tests/ 2>&1 | head -20
```

- 其他 `call("tool.xxx", ...)` 也都要加 `name=` 显式形参
- 这是机械改动，按需更新

- [ ] **Step 6: Commit**

```bash
cd D:/Projects/SelfLearn && git add backend/src/selflearn/tools/protocol.py backend/src/selflearn/agents/builtin/exercise_agent.py backend/src/selflearn/tools/builtin/fetch_template.py && git commit -m "refactor(tools): ToolRegistry.call 把 name 收进 kwargs 消除冲突"
```

---

## Task 8: observability 链——统一 director/exercise/review 的 failure 上报

**Files:**
- Modify: `backend/src/selflearn/agents/builtin/director_agent.py`
- Modify: `backend/src/selflearn/agents/builtin/exercise_agent.py`
- Modify: `backend/src/selflearn/agents/builtin/review_agent.py`
- Reference: `backend/src/selflearn/progress/stages.py`, `backend/src/selflearn/progress/stream.py`

**问题：** 调试时还得靠 `import traceback; log.error(..., tb=...)` 临场 hack。AOP Hook 在 envelope/llm/progress 装了，但 director 内部 try/except 没接。

**目标：** 把 `_run_inner` / `run_sync` / `review` 内部的 except 统一走 `progress_publish(trace_id, ProgressEvent(stage=Stage.FAILED, ..., payload={code, message, tb}))`，让前端 SSE 能直接看到完整 traceback。

- [ ] **Step 1: 看 progress/stages.py + stream.py 的 FAILED event 现有结构**

```bash
cd D:/Projects/SelfLearn && cat backend/src/selflearn/progress/stages.py | head -40
cd D:/Projects/SelfLearn && grep -n "FAILED\|failed" backend/src/selflearn/progress/stages.py
```

确认 `Stage.FAILED` 存在 + payload 字段约定（现状是 `code` / `message`）。

- [ ] **Step 2: 写失败测试 — director._run_inner 内部 except 推 FAILED 事件含 tb**

文件 `backend/tests/unit/test_director_tryexcept.py` 末尾追加：
```python
async def test_director_failed_event_contains_traceback() -> None:
    """_run_inner 内部异常时，FAILED 事件 payload 必须含 traceback 字段。"""
    from selflearn.agents.builtin.exercise_agent import ExerciseAgent
    from selflearn.agents.builtin.director_agent import DirectorAgent
    from selflearn.core.envelope import ActorRef, Envelope
    from selflearn.progress.stages import ProgressEvent, Stage

    published: list[ProgressEvent] = []

    async def collect(trace_id: str, ev: ProgressEvent) -> None:
        published.append(ev)

    with patch.object(ExerciseAgent, "run_sync",
                      new=AsyncMock(side_effect=ValueError("boom_with_tb"))), \
         patch("selflearn.agents.builtin.director_agent.progress_publish",
               new=AsyncMock(side_effect=collect)):
        agent = DirectorAgent()
        env = Envelope(
            action="skill.execute",
            sender=ActorRef(type="gateway", id="g"),
            target=ActorRef(type="skill", id="skill.director.start"),
            payload={"student_id": "00000000-0000-0000-0000-000000000004"},
            trace_id="dir-tb-test",
        )
        with pytest.raises(Exception):
            await agent.run(env)

    failed = [e for e in published if e.stage == Stage.FAILED]
    assert failed, "FAILED event 必须推"
    assert failed[0].payload.get("tb"), "FAILED payload 必须含 tb 字段"
    assert "boom_with_tb" in failed[0].payload["tb"]
```

- [ ] **Step 3: 跑测试确认红**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_director_tryexcept.py::test_director_failed_event_contains_traceback -v --no-header
```

Expected: FAIL with `KeyError: 'tb'` 或断言失败

- [ ] **Step 4: 改 director_agent.py 内部 _emit_failed + run() 的 except**

替换 `backend/src/selflearn/agents/builtin/director_agent.py` line 50-53：
```python
except Exception as e:  # noqa: BLE001
    import traceback as _tb
    tb_str = _tb.format_exc()
    await self._emit_failed(trace_id, "internal_unhandled", repr(e), tb=tb_str)
    log.error("director.unhandled_exception", trace_id=trace_id, error=repr(e), tb=tb_str)
    raise AppError(ErrorCode.INTERNAL, "Director 处理失败", trace_id=trace_id) from e
```

`_emit_failed` 签名加 `tb` 参数：
```python
async def _emit_failed(self, trace_id: str, code: str, message: str, tb: str | None = None) -> None:
    payload: dict[str, object] = {"code": code, "message": message}
    if tb:
        payload["tb"] = tb
    await progress_publish(trace_id, ProgressEvent(
        stage=Stage.FAILED, status="failed", payload=payload,
    ))
```

删除 director 内部临场的 `import traceback; log.error(..., tb=...)`（Task 4 commit 加的临时调试），统一走 _emit_failed。

- [ ] **Step 5: 跑测试确认绿**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest tests/unit/test_director_tryexcept.py -v --no-header
```

Expected: 2 passed（原 1 + 新 1）

- [ ] **Step 6: ExerciseAgent / ReviewAgent 同样改造**

按需把 exercise_agent.run_sync / review_agent.review 的 except 推 ProgressEvent FAILED。**这一步是 nice-to-have，可选**——如果时间紧，至少在 commit message 里标记 TODO。

- [ ] **Step 7: 跑全套 + Commit**

```bash
cd D:/Projects/SelfLearn/backend && .venv/Scripts/python.exe -m pytest -v --no-header 2>&1 | tail -5
cd D:/Projects/SelfLearn && git add backend/src/selflearn/agents/builtin/director_agent.py backend/tests/unit/test_director_tryexcept.py && git commit -m "feat(observability): FAILED 事件 payload 加 tb 字段，前端 SSE 可读"
```

---

## Task 9: smoke_mvp.sh 端到端验证 + 修任何真问题

**Files:**
- Modify: `backend/scripts/smoke_mvp.sh`
- Verify: 全套后端 + 前端可跑

**问题：** smoke_mvp.sh 第 5 步（POST /api/level/start）+ 第 7 步（submit）从来没真正端到端跑通过。

- [ ] **Step 1: 重启 worker + gateway 用最新代码**

```bash
powershell -NoProfile -Command "Get-CimInstance Win32_Process -Filter \"Name='python.exe'\" | Where-Object { \$_.CommandLine -like '*selflearn.main*worker*' -or \$_.CommandLine -like '*uvicorn*selflearn*' } | ForEach-Object { Stop-Process -Id \$_.ProcessId -Force -ErrorAction SilentlyContinue }; Start-Sleep -Seconds 3"
docker exec selflearn-rabbitmq rabbitmqctl purge_queue agent.mvp.work 2>&1 | tail -1
cd D:/Projects/SelfLearn/backend && find src/selflearn -name "__pycache__" -type d 2>/dev/null | xargs rm -rf
cd D:/Projects/SelfLearn/backend && nohup .venv/Scripts/python.exe -m selflearn.main --role worker > /tmp/worker.log 2>&1 &
cd D:/Projects/SelfLearn/backend && nohup .venv/Scripts/uvicorn.exe selflearn.gateway.app:create_app --factory --port 8000 > /tmp/gateway.log 2>&1 &
sleep 5
```

- [ ] **Step 2: 跑 smoke_mvp.sh**

```bash
cd D:/Projects/SelfLearn/backend && bash scripts/smoke_mvp.sh 2>&1 | tail -50
```

Expected: 7 步全部 PASS，DB 出现 1 个新 level + N 个 exercises

- [ ] **Step 3: 任何失败的步骤单独修**

可能问题：
- profile SSE 超时 → 看 worker 日志 + 调整 timeout
- level.start 仍 lint 拒收 → 回到 Task 6 Step 4
- submit 报 404 → 看 level_id 是否有

- [ ] **Step 4: 最后一次跑 smoke_mvp.sh 全绿 + 看 DB 验证**

```bash
docker exec selflearn-postgres psql -U selflearn -d selflearn -c "SELECT level_id, status, form, created_at FROM levels ORDER BY created_at DESC LIMIT 3;"
docker exec selflearn-postgres psql -U selflearn -d selflearn -c "SELECT level_id, COUNT(*) FROM exercises GROUP BY level_id ORDER BY level_id DESC LIMIT 3;"
docker exec selflearn-postgres psql -U selflearn -d selflearn -c "SELECT level_id, verdict, score FROM review_results ORDER BY created_at DESC LIMIT 3;"
```

Expected: 至少 1 个 level + 2-4 个 exercises + 1 个 review_result

- [ ] **Step 5: Commit（如有改动）**

```bash
cd D:/Projects/SelfLearn && git add backend/scripts/smoke_mvp.sh && git commit -m "test(smoke): smoke_mvp.sh 端到端通过 level.start + submit"
```

---

## Task 10: 写完成报告 docs/superpowers/reports/2026-07-14-t13.5.md

**Files:**
- Create: `docs/superpowers/reports/2026-07-14-t13.5.md`

- [ ] **Step 1: 写报告骨架**

```markdown
# T13.5 报告 — Stage 4 端到端真正跑通 + 4 处欠债清账

**日期：** 2026-07-14
**对应 spec：** docs/superpowers/specs/2026-07-13-stage4-demo-integration-design.md
**对应 plan：** docs/superpowers/plans/2026-07-14-t13.5-demo-truly-end-to-end.md

## TL;DR
[2 句话说清本计划做了什么、效果如何]

## 修了什么
- prompt 模板：exercise_generation_v1 stub → 完整结构说明（强制 array 顶层 + 6 字段必填）
- ToolRegistry：name 形参 → 收进 kwargs 消除冲突
- observability：FAILED 事件 payload 加 tb 字段
- 4 处测试债清账

## 测试结果
- pytest：N passed (link to run log)
- smoke_mvp.sh：7 步全过
- DB 校验：levels 出现 N 条 / exercises N 条 / review_results N 条

## 已知遗留
- 节点 status 翻转（MapNode.active → completed 视觉）推到 T14 之后
- 5174 CORS、worker 'smoke' warning、student.py 死代码 是 Stage 4 收尾件，未在本 plan 处理
- 流式 reasoning 仍走 chat_stream 拼 content（性能略低、无功能影响）
```

- [ ] **Step 2: 填实际数据 + Commit**

```bash
cd D:/Projects/SelfLearn && git add docs/superpowers/reports/2026-07-14-t13.5.md && git commit -m "docs(t13.5): 报告"
```

---

## 执行后状态

- pytest 全部通过
- smoke_mvp.sh 端到端绿
- levels / exercises / review_results 表有真实记录
- observability 链能直接报 traceback 到前端 SSE
- ToolRegistry 设计无 name 冲突
- 4 处测试债清账

**不再阻塞的下一阶段：** T14 Playwright e2e + 节点 status 翻转 + 收尾件。
