# T13.5 报告 — Stage 4 Demo 端到端真正跑通 + 多处技术债清账

**日期**：2026-07-14
**对应 spec**：docs/superpowers/specs/2026-07-13-stage4-demo-integration-design.md
**对应 plan**：docs/superpowers/plans/2026-07-14-t13.5-demo-truly-end-to-end.md

## TL;DR

Stage 4 demo 端到端**真正跑通**——`/api/level/start` 走完 Exercise → Review → Profile → 写库全链路，DB 中出现真实的 level / exercises / review_results / profile_snapshots 记录。同时清掉 4 处技术债：测试签名漂移、V1.1 fix 不完整、ToolRegistry name 冲突、observability 链缺失。

## 修了什么（按 plan 任务顺序）

### Task 1: 同步 test_exercise_agent 签名
- 前序 commit 改了 `ExerciseAgent.run_sync(env, node=fake_node)` → `(env, node_id, kp_title, difficulty="medium")`，但测试没改
- commit `888e0c1`：两个测试都改用新签名 + 删 fake_node fixture + 删 UUID import

### Task 2: 验证 test_director_tryexcept + 修复 V1.1 不完整
- 验证发现 director_agent.py 的 `except Exception` 分支漏调 `_emit_failed`
- 这是 Stage 3 V1.1 fix 的回归保护缺失——从 commit e56fa83 至今可能从未真正 PASS 过
- commit `5f7d55b`：src 加一行 `await self._emit_failed(...)` + 重写测试用 3 层 mock（get_skill / ExerciseAgent / get_session_factory）

### Task 3.1: 修 pre-existing Profile 测试失败
- 验证 test_api_gaps 时发现 3 个测试从 commit 5272277 起就 fail
- Root cause：fake 函数签名 `student_id: uuid.UUID` 错了，路由/repo 实际是 `str`
- commits `9e0a20e` + `9f66f46`：改签名 + 修 unreachable 死代码

### Task 4: 补 LLM adapter streaming+reasoning 测试
- commit `9e8e0bf`：加 `test_chat_with_reasoning_uses_streaming_and_assembles_content` 覆盖
  - chat(reasoning=True) 走 chat_stream 拼 content 路径
  - reasoning-only chunk（无 choices 字段）安全跳过

### Task 5: ExerciseAgent MockLLMAdapter 链路测试
- commit `326c9fa`：用 `_StaticMockAdapter` 子类覆写 chat 返回合规 JSON，让 prompt→LLM→lint 真实链路跑通

### Task 6: 修 prompt 模板 + lint_json 回归保护
- commit `8fce250`：prompt 从 stub 改完整 YAML（type / version / body block scalar）
  - 显式说明"顶层必须是 array"
  - 列出 6 个必填字段
  - 含严格对齐 schema 的样例 JSON
- 新增 `backend/tests/unit/test_lint_json.py`：4 个真验证测试（不 mock）

### Task 7: ToolRegistry 重构
- commit `8ee3931`：ToolRegistry.call 把 name 收进 kwargs（pop 出去）
- 修 3 处 src call sites（exercise / review / fetch_template）
- 修 2 处 tests call sites
- fetch_template 双 key 兼容保留

### Task 8: observability 链 FAILED 事件加 tb
- commit `5098fb3`：director / exercise / review 三个 Agent 的 except 统一推 ProgressEvent
- director._emit_failed 加 `tb: str | None = None` 参数
- 3 个新测试覆盖：`test_director_failed_event_contains_traceback` / `test_exercise_agent_unhandled_exception_publishes_failed` / `test_review_agent_unhandled_exception_publishes_failed`

### Task 9: smoke_mvp.sh 端到端验证
- **端到端真正跑通**——8 步全 PASSED
- 修 3 处真问题：
  1. SSE 关闭条件（level.py / profile.py）：从 `stage.value in ("completed", "failed")` 改为 `status in ("completed", "failed")`
  2. profile_snapshots 表缺失：手动 `CREATE TABLE`（Alembic 路径未走通，留作后续）
  3. smoke_mvp.sh 第 8 步 emoji ✓ 编码问题：改 `OK`
- commits `bbbf4e0` + `1698c77`

## 测试结果

### pytest
- task 完成后 pytest 全套：**106 passed**（含 1 个新增 test_lint_json.py 4 个 + 其他 task 新增）
- pre-existing 失败：test_plan_agent（base commit 也失败）+ test_scheduler_target_id_routing（test-ordering flakiness，与本 plan 无关）
- review 阶段 reviewer 独立验证：plan_agent 在 base commit stash 后单跑仍 FAIL（确认 pre-existing）；scheduler 隔离跑 PASS（确认 flakiness）

### mypy
- `mypy src/selflearn` → `Success: no issues found in 85 source files`

### smoke_mvp.sh
- 8 步全 PASSED：
  - 1) seed KP ✓
  - 2) POST /api/profile/build ✓
  - 3) SSE profile init (progress + completed) ✓
  - 4) POST /api/map/generate ✓
  - 5) POST /api/level/start ✓
  - 6) SSE director (director → exercise → review → completed) ✓
  - 7) POST /api/level/{id}/submit ✓
  - 8) level.status=completed ✓

### DB 验证
- `levels`：端到端生成新 level（status=completed，form=exercise）
- `exercises`：每个 level 3 道题
- `review_results`：verdict=needs_fix, score=0.6
- `profile_snapshots`：1 条 level_completed trigger

### LLM
- 阿里云 MaaS DeepSeek 真实调通，prompt 强化后 lint_json 不再拒
- review 真给出 verdict+score
- profile 真增量更新（kb delta=-0.03, as delta=-0.02）

## 已知遗留

1. **profile_snapshots 表用手动 CREATE**：Alembic migration 路径未走通，留作后续 Stage 4 收尾 task。
2. **smoke_mvp.sh 仍用 `uv run`**：Windows 环境没装 uv，靠 bash fallback。建议下次清理时全替换成 `.venv/Scripts/python.exe`。
3. **level.py / profile.py SSE 重复**：两处 `_stream_events` / `event_gen` 都改了同一 bug，codebase 重复。建议提取公共 helper。
4. **节点 status 翻转**（MapNode.active → completed 视觉）推到 T14 之后。
5. **5174 CORS、worker 'smoke' warning、student.py 死代码** 是 Stage 4 收尾件，未在本 plan 处理。
6. **流式 reasoning 仍走 chat_stream 拼 content**（性能略低、无功能影响）。
7. **test_scheduler_target_id_routing.py 全套跑 flakiness** 是 event loop 共享状态问题，与本 plan 无关但值得修。

## 不再阻塞的下一阶段

- T14 Playwright e2e + 节点 status 翻转 + Stage 4 收尾件
- 用户已确认：13.5 完成后直接做 T14（不停下来等确认）
- 目标：把代码弄干净 + 观测链路完整，避免以后大范围 grep 才能修复

## Commit 列表（按时间顺序）

```
888e0c1 test: 同步 ExerciseAgent 新签名 (node_id/kp_title 字符串)
5f7d55b fix(director): V1.1 fix 闭环 — except Exception 也推 FAILED + 测试覆盖 run_sync 路径
9e0a20e test: 修 Profile 测试 fake 函数签名 — student_id 应该是 str 不是 UUID
9f66f46 test: 移除 unreachable 旧 return 行（fake_recent_snapshots）
9e8e0bf test: 补 LLM adapter reasoning+streaming 路径测试
326c9fa test: ExerciseAgent 真实过 MockLLMAdapter 链路
8fce250 fix(prompt): exercise_generation_v1 强化 JSON 顶层 array + 6 字段必填 + lint_json 回归测试
8ee3931 refactor(tools): ToolRegistry.call 把 name 收进 kwargs 消除冲突
5098fb3 feat(observability): director/exercise/review except 统一推 FAILED 事件 + tb payload
bbbf4e0 fix(gateway): SSE 完成/失败判定改按 status 而非 stage — 让 ProfileAgent (stage=profile,status=completed) 也能触发 SSE 关闭
1698c77 fix(worker): reply envelope 早退防止自循环 (skill.completed 跳过 dispatch)
```

11 个 commits，2 天工作，0 倒退，Stage 4 demo 真正端到端跑通。